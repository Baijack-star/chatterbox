## 开放API (Open APIs)

HeyGem 项目在本地部署并通过 Docker 启动其服务后，会暴露一系列 API 端口，允许开发者通过编程方式与其核心功能进行交互。这些 API 为集成 HeyGem 的数字人能力到第三方应用或自动化工作流提供了便利。

**概述 (Overview):**

*   一旦相关的 Docker 容器成功启动，API 服务将在本地主机的指定端口上可用。
*   开发者可以通过标准的 HTTP 请求来调用这些 API 接口。
*   具体的端口号和API路径应参考官方文档，以下示例仅为示意。

**模特训练 API (Model Training API):**

此 API 主要负责处理原始视频，提取用于数字人模型训练所需的特征。

*   **功能描述:**
    *   接收用户上传的视频文件。
    *   对视频进行预处理，可能包括格式转换、帧率调整、人脸检测与裁剪等。
    *   提取关键的面部特征和声音特征，用于后续的模型克隆和训练。
*   **示例端点 (Example Endpoint):**
    *   `POST http://127.0.0.1:18180/v1/preprocess_and_train` (实际路径和方法可能有所不同)
    *   请求体中通常需要包含视频文件的路径或内容，以及可能的配置参数。

**音频合成 API (Audio Synthesis API):**

该 API 用于根据输入的文本和克隆的声音特征（或指定的参考音频）来生成目标语音。

*   **功能描述:**
    *   接收文本内容和指定的声音特征标识（例如，从模特训练 API 获取的模型 ID 或声音样本）。
    *   调用后端的 TTS 引擎（如 fish-speech-ziming）和声音克隆模块，合成目标语音。
    *   返回生成的音频数据或其访问路径。
*   **示例端点 (Example Endpoint):**
    *   `POST http://127.0.0.1:18180/v1/invoke` (实际路径和方法可能有所不同)
    *   请求体中通常包含要转换的文本、声音模型标识以及其他合成参数（如语速、语调等）。

**视频合成 API (Video Synthesis API):**

此 API 负责将生成的音频与数字人模型的视觉信息结合，最终合成为口播视频，并提供任务状态查询。

*   **功能描述:**
    *   **提交合成任务:** 接收音频数据（或其引用）和数字人模型标识，启动视频合成过程。这个过程通常是异步的。
    *   **查询任务进度:** 允许用户查询已提交的视频合成任务的当前状态（如排队中、处理中、已完成、失败等）和结果。
*   **示例端点 (Example Endpoints):**
    *   提交视频合成任务: `POST http://127.0.0.1:8383/easy/submit` (实际路径和方法可能有所不同)
        *   请求体通常包含音频信息、数字人模型标识、背景、布局等视频配置参数。
    *   查询视频合成进度: `GET http://127.0.0.1:8383/easy/query?task_id=<your_task_id>` (实际路径和参数可能有所不同)
        *   需要提供任务提交后返回的任务 ID。

**使用建议:**

*   开发者在调用这些 API 前，应仔细阅读 HeyGem 项目的官方 API 文档，了解详细的请求/响应格式、参数说明、认证方式（如果需要）以及错误代码。
*   考虑到模型训练和视频合成可能是耗时操作，API 设计上通常会采用异步处理模式，开发者需要相应地处理回调或轮询逻辑。

通过这些开放 API，HeyGem 使得其强大的数字人生成能力可以被更广泛地集成和应用。
